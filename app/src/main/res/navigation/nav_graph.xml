<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.moviedataapp.home.HomeFragment"
        android:label="Movie Data App"
        tools:layout="@layout/fragment_home">
        <action
            android:id="@+id/action_homeFragment_to_moviesFragment"
            app:destination="@id/moviesFragment" />
    </fragment>
    <fragment
        android:id="@+id/moviesFragment"
        android:name="com.example.moviedataapp.movies.MoviesFragment"
        tools:layout="@layout/fragment_movies">
        <argument
            android:name="selectedTopicAPI"
            app:argType="string" />
        <action
            android:id="@+id/action_moviesFragment_to_movieDetailFragment"
            app:destination="@id/movieDetailFragment" />
        <action
            android:id="@+id/action_moviesFragment_to_homeFragment"
            app:destination="@id/homeFragment"
            app:popUpTo="@id/homeFragment"
            app:popUpToInclusive="true" />
    </fragment>
    <fragment
        android:id="@+id/movieDetailFragment"
        android:name="com.example.moviedataapp.detail.MovieDetailFragment"
        android:label="Movie Detail"
        tools:layout="@layout/movie_detail_fragment">
        <argument
            android:name="selectedMovieId"
            app:argType="long" />
    </fragment>

</navigation>